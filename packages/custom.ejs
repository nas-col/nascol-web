```{=html}

<style>
    #packages {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(min(200px, 100%), 1fr));
        grid-template-rows: max-content;
        grid-gap: 10px;
    }

    .item {
        padding: 1%;
    }

    .item img {
        max-height: 100%;
        max-width: 100%;
        position: relative;
        transform: translateY(-50%);
    }

    .card-img-top {
        aspect-ratio: 1 / 1;
    }

    .card-img-top a {
        position: relative;
        top: 50%;
    }
</style>
<% for (const item of items) { %>

    <h2><%- item.category %></h2>
    <div id="packages">
        <% for (const tile of item.tiles) { %>
            <div class="item">
                <p class="card-img-top">
                    <a href="<%= tile.href %>">
                    <img src="logos/<%= tile.img %>" class="thumbnail-image
                    card-img">
                    </a>
                </p>
                <h3 style="text-align: center;">
                    <%= tile.name %>
                </h3>
                <p style="text-align: left; font-size: small;">
                    <%= tile.description %>
                </p>
            </div>
        <% } %>
    </div>
<% } %>
```


